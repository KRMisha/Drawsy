import { JunctionSettings } from './junction-settings';

describe('JunctionSettings', () => {
    it('should create an instance', () => {
        expect(new JunctionSettings()).toBeTruthy();
    });
});
