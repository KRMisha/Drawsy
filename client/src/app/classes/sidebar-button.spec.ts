import { SidebarButton } from '@app/classes/sidebar-button';

describe('SidebarButton', () => {
    it('should create an instance', () => {
        expect(new SidebarButton()).toBeTruthy();
    });
});
