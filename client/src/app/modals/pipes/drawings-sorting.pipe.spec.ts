import { DrawingsSortingPipe } from './drawings-sorting.pipe';

describe('DrawingsSortingPipe', () => {
    it('create an instance', () => {
        const pipe = new DrawingsSortingPipe();
        expect(pipe).toBeTruthy();
    });
});
