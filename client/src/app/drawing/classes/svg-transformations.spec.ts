import { SvgTransformations } from '@app/drawing/classes/svg-transformations';

describe('SvgTransformations', () => {
    it('should create an instance', () => {
        expect(new SvgTransformations()).toBeTruthy();
    });
});
