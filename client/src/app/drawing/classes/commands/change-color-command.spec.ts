import { ChangeColorCommand } from './change-color-command';

describe('ChangeColorCommand', () => {
    it('should create an instance', () => {
        expect(new ChangeColorCommand()).toBeTruthy();
    });
});
