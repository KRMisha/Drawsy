import { AppendElementsCommand } from './append-elements-command';

describe('AppendElementsCommand', () => {
    it('should create an instance', () => {
        expect(new AppendElementsCommand()).toBeTruthy();
    });
});
