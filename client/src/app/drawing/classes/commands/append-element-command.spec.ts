import { AppendElementCommand } from './append-element-command';

describe('AppendElementCommand', () => {
    it('should create an instance', () => {
        expect(new AppendElementCommand()).toBeTruthy();
    });
});
