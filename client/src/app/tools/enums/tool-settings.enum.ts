export enum ToolSetting {
    Size,
    JunctionSettings,
    StrokeType,
    Texture,
    PolygonSideCount,
}

export enum Textures {
    Texture1 = 1,
    Texture2,
    Texture3,
    Texture4,
    Texture5,
}

export enum StrokeTypes {
    FillWithBorder,
    FillOnly,
    BorderOnly,
}
