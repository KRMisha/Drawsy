export enum ToolSetting {
    Size,
    StrokeSize,
    EraserSize,
    JunctionSettings,
    StrokeType,
    BrushTexture,
    PolygonSideCount,
    SpraySpeed,
    SprayRadius,
}

export enum StrokeType {
    FillWithBorder,
    FillOnly,
    BorderOnly,
}
