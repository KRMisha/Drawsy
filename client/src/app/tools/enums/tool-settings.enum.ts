export enum ToolSetting {
    Size,
    StrokeSize,
    EraserSize,
    JunctionSettings,
    StrokeType,
    Texture,
    PolygonSideCount,
    SpraySpeed,
    SprayRadius,
}

export enum Texture {
    Graffiti = 1,
    Blur,
    Shadow,
    Matte,
    Fabric,
}

export enum StrokeType {
    FillWithBorder,
    FillOnly,
    BorderOnly,
}
