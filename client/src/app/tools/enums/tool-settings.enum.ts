export enum ToolSetting {
    Size,
    JunctionSettings,
    StrokeType,
    Texture,
}

export enum Texture {
    Blur = 1,
    Graffiti,
    Shadow,
    Matte,
    Fabric,
}

export enum StrokeType {
    FillWithBorder,
    FillOnly,
    BorderOnly,
}
