export enum ToolSetting {
    Size,
    StrokeSize,
    EraserSize,
    JunctionSettings,
    StrokeType,
    Texture,
    PolygonSideCount,
    Density,
}

export enum Texture {
    Blur = 1,
    Graffiti,
    Shadow,
    Matte,
    Fabric,
}

export enum StrokeType {
    FillWithBorder,
    FillOnly,
    BorderOnly,
}
